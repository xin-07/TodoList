# TodoList

> 每次 AI 对话开始前，必须先阅读项目根目录的 `AI_CONSTITUTION.md`（`d:\Project\C#_project\TodoList\AI_CONSTITUTION.md`），所有操作都必须遵守其中每一条规定。

AI 不能自己启动项目，启动项目只能由用户来启动，AI 只告诉用户启动指令是什么。

## 变更后验证约定

AI 每次修改代码之后必须执行变更后验证（编译 + 单元测试，不启动应用），权威定义见宪法 Article 5 细则：`docs/rule/article-05-post-change-verification.md`。
