# AI Collaboration Constitution

> This document is the supreme law for all AI-assisted development. Every modification must comply. Violations are errors, not suggestions. Treat this as permanent infrastructure, not a guideline.

## 核心原则

- 任何 AI 行为——写代码、改文档、建文件、执行命令——都必须通过这一判断：*这符合宪法的每一适用条款吗？*
- 若答案不确定，应停止，直至获得确定性，不得推进。
- 本宪法整合了通用软件工程准则（文档-代码共同维护、根因分析、单一权威源、no-patchwork 等）与本项目特定约定（commit 展示确认、任务三分法、spec 领域目录）。

---

## Article 1. 文档与代码共同维护（commit 必须含 Why/What）

- **规则**：代码与文档同等重要，必须共同维护，二者不得相互脱节。影响生产行为的代码变更，其 commit 必须含 Why/What（根因三选一：**design / code / test wrong**）。
- **释义**：
  - 仅文档变更（docs）单独提交时，可省略 Why/What。
  - 代码变更的 commit 必须给出 `why:`（design flaw / implementation bug / test gap）与 `what:`（变更内容）。
  - 提交前自问：*设计错了？代码错了？测试错了？* 三者均为有效答案。
- **禁止**：
  - 不说明 bug 成因就写 `"fix bug"`。
  - 不带根因分析的 `"hotfix"`。
  - 只描述现状、不解释成因的文档；用文档掩盖已知不一致而非先修复。
- **执行**：Git `commit-msg` 钩子拒绝缺失 `why:` / `what:` 或包含通用无信息占位符的提交。

---

## Article 2. 禁止"静默三件套"

禁止 `@ts-ignore`、空 `catch{}`、返回 null 的 stub。任何吞错/绕过的写法都必须改为显式处理并给出根因。

---

## Article 3. 没有"以后"——技术债必须显式跟踪

- **规则**：当前任务范围内的工作必须现在完成；无法立即解决的技术债，必须显式、结构化成带截止期限与负责人的记录，不得以裸 TODO 或模糊承诺推给"将来"。
- **禁止**：裸 `// TODO: fix later`（无截止日期、无 owner、无原因）。
- **正确**：写成 `// DEFERRED：reason，defer-to：YYYY-MM-DD，owner：name`。
- **执行**：提交前债务检查器（linter）在遇到过期或未格式化的 TODO / DEFERRED 时报错。

---

## Article 4. commit 前必须展示确认

- 执行 commit 前，必须先向用户展示拟提交的 commit 信息（subject + Why/What），等待用户明确确认后，才能执行 `git commit`；禁止未经用户审查直接提交。
- 纯文档（docs / chore / style）变更若用户已事先知晓其内容，可省略逐条展示，但仍需告知用户后再提交。

---

## Article 5. 任务三分法与 SPEC 领域目录

- 每个任务在执行前必须归为三类之一，并按对应方式执行：
  - **simple**：单文件修改。直接小改，不写 SPEC。
  - **complex**：多文件/多模块改动。先写 SPEC，用户确认后再做代码实现。SPEC 存放于项目根 `docs/spec/<问题领域>/`，文件夹**按问题领域分类**（如 `feature` 功能类、`docs` 文档类、`test` 测试类等），不直接使用任务类型（simple / complex / research）作为目录名；**每个问题只允许一个文档**，文件直接以「任务名-状态」命名，状态必须为 `DRAFT` / `IN_PROGRESS` / `DONE` 之一，如：
    - `docs/spec/feature/task-DRAFT.md`（起草中）
    - `docs/spec/feature/task-IN_PROGRESS.md`（实现中）
    - `docs/spec/feature/task-DONE.md`（已完成，状态推进时同步改名）
  - **research（研究）**：只写文档/研究报告，不写实现代码。
- 任何功能修改/新增完成后，都必须交给用户测试验证，获得用户反馈后才算完成；AI 不自行启动应用。
- AI 侧变更后验证（编译 + 单元测试）为强制义务，细则见 [docs/rule/article-05-post-change-verification.md](docs/rule/article-05-post-change-verification.md)。

---

## Article 6. 每次变更都要做根因分析

- **规则**：每次修改都必须说明问题的根因、影响范围和预期结果。修复缺陷时，绝不只描述症状或复述用户看到的错误。
- **为何**：杜绝针对症状的打补丁。不做根因诊断的变更会添加任意条件或绕过故障，使同类失败必然复发。
- **禁止**：只捕获异常而不诊断触发原因；在调用处临时加 null 检查而不查明上游状态为何失效。
- **正确**：将状态生命周期追溯到非法变更来源，直接在源头修复 schema、生命周期或协议。
- **执行**：代码评审与事后审计要求 bug 工单和 commit 记录"五个为什么"（5 Whys）。

---

## Article 7. 设计决策即契约

- **规则**：已确认的设计决策——模块边界、接口签名、数据约束、错误处理约定、架构权衡——对后续实现具有约束力。
- **为何**：工作记忆会跨会话衰减。已确认的设计可防止 AI 临时重造 schema 或悄然破坏架构边界。
- **禁止**：常规 bug 修复中悄悄改动公共签名或数据库 schema；因局部需求不便就在已批准契约下实现有分歧的逻辑。
- **正确**：明确提出契约变更 → 获得用户批准 → 记录 ADR/SPEC 修订 → 再实现代码。
- **执行**：自动化 API 差异检查与契约测试在未声明的破坏性签名漂移时失败。

---

## Article 8. 命名反映身份，而非历史

- **规则**：文件、目录、模块、接口、变量、配置的名称必须表达当前职责和领域身份，而非历史迁移痕迹。
- **禁止**：在名称上添加 `new` / `old` / `temp` / `final` / `v2` / `enhanced` / `wrapper` 等前缀或后缀；保留带有活跃感的废弃产物。
- **正确**：按实际领域角色命名抽象。取代旧物时，重命名旧组件或彻底退役。
- **执行**：提交前 grep 规则拦截新声明中被禁的临时标记词（`new/old/temp/v2`）。

---

## Article 9. 配置必须单一权威源

- **规则**：每个配置值、环境常量和业务规则都必须且只能有一处权威声明；次要位置只能从中派生、生成或同步。（本项目已据此：数据路径统一在 `config.json` 定义。）
- **禁止**：在多个互不相关的文件中硬编码同一端口、URL 或功能开关；建立独立配置查找表而非引入权威定义。
- **正确**：在规范 config/schema 文件中定义 → 直接引用，或经自动化构建脚本生成次要配置。
- **执行**：提交前同步器校验从属配置与上游规范文件一致。

---

## Article 10. 废弃文档必须重定向

- **规则**：任何被弃用、取代或迁移的规范、规则、ADR 或分析文档，都必须保留明确的跳转提示，指向现行权威文档。
- **禁止**：不留下墓碑标记就删除或弃置文档；让废弃文档看起来仍像可行的实现指南。
- **正确**：在文档头部放置醒目标记：
  `> **重定向**：本文档已被 [XXX](../path/to/xxx.md) 取代。请勿将其作为实现依据。`
- **执行**：文档索引脚本校验所有交叉链接，并标记未重定向的废弃文件。

---

## Article 11. 禁止基于时间或非确定性外部状态做分支

- **规则**：业务逻辑分支不得依赖当前墙钟时间、系统日期、硬件环境、随机熵或不受控的外部条件——除非该依赖明确属于业务领域且带有可 mock、可测试的注入边界。
- **禁止**：在核心业务逻辑中调用 `Date.now()` / `new Date()` / `Math.random()` 或直接检查宿主机；用 `setTimeout()` / `sleep()` 制造人为延迟作同步机制（延迟仅允许用于真实 UI 动画，绝不用于业务门控或并发协调）。
- **正确**：通过依赖注入或参数传递注入确定性时钟/熵提供者。
- **执行**：Linter 标记领域逻辑包中对非确定性原语的直接访问。

---

## Article 12. 禁止打补丁式修改（No-Patchwork）

- **规则**：对既有文档或代码模块的修改必须意图连贯，不得是一次性调整——引入概念就该承担其完整生命周期。
- **禁止**：给本非用于枚举规则的字段加规则计数器；改文档统计值来掩盖不一致而不修根因；加括号解释来替代修复不完整/误导的描述；在既有权威定义可提供数据处另建查找映射。
- **正确**：发现新事实 → 加入规范定义而非影子副本；发现设计问题 → 在权威源修复而非在消费端绕开；发现模式 → 在相关规则文件正式记录。
- **执行**：代码评审与 PR 门禁拒绝补丁掩盖症状、却让底层 schema 仍不一致的提交。

---

## Article 13. 预留项目特定指令

- **规则**：通用工程规则由上述条款管辖；本条款预留给特定于本项目部署的领域约束、合规或运行时限制（如零 PII 日志、离线优先同步等硬性领域边界）。
- **禁止**：在没有事实生产依据的情况下杜撰虚构的项目要求。

---

## 执行

每次 AI 会话启动时，AI 必须阅读本文件并确认：

> **"我已阅读 AI_CONSTITUTION.md，并将遵守所有条款。我不会执行任何违反这些规则的修改。"**

若事后发现违规，必须先立即纠正，再继续会话。

---

## 例外

唯一例外是项目所有者的明确书面批准。任何例外必须记录在与违规变更相同的提交中，注明所覆盖的具体条款及可验证理由。

---

## 审查

- 本宪法应在发现新类别违规、项目级约束变化，或 AI 会话识别出规则缺口时审查更新。
- *对本文档的每一次更新本身也受所有条款约束。*